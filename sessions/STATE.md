# Состояние проекта

> Обновляется в конце каждой сессии. Описывает «как есть сейчас», а не историю
> изменений — старое перезаписывается. Первым делом в новой сессии агент читает
> именно этот файл.

**Обновлено:** 2026-08-17, сессия 4 — **финальное состояние, работа завершена**

Проект сдан. Репозиторий опубликован: https://github.com/Domex666/dz1
Дальнейших сессий не планируется; всё ниже описывает то, что ушло в сдачу.

## Готово

- **Контекст.** Часть II `AGENTS.md` заполнена под задачу: стек, версия PHP, порт,
  архитектурные требования из плагина `laravel-arch`, категоричные запреты,
  случаи обязательной остановки, чек-лист перед финальным ответом.
- **Спецификация.** `SPEC.md` написан до кода и приведён в соответствие с фактом
  по итогам аудита: сценарии, состояния, контракт API, нецели, допущения,
  открытые вопросы, критерии приёмки.
- **Наивный прогон.** `naive/index.php` — снапшот, не исправляется.
- **Реализация.** 43 файла PHP в `app/` по слоям плюс сборка и точка входа,
  шесть эндпоинтов, файловое хранилище.
- **Тесты.** 152 теста, 285 проверок, все зелёные. Вывод — `docs/evidence/phpunit.log`.
- **Ручная проверка.** `scripts/smoke.sh`, 25 сценариев и 34 сверки по живому сервису,
  с ненулевым кодом выхода при расхождении и отказом работать поверх непустого
  хранилища. Вывод — `docs/evidence/smoke.log`.
- **Независимый аудит, три захода.** Первый — три агента: контракт, правила
  и архитектура, качество тестов; 40 находок, 28 приняты (`session-2.md`).
  Второй — четыре агента, добавлены чистый запуск по README и аудит комплектности;
  два настоящих дефекта (`session-3.md`). Третий — тот же состав повторно;
  ещё четыре дефекта и шестнадцать дыр в проверках (`session-4.md`).
- **Перепроверка мутациями.** Каждый заход заканчивался прогоном мутаций
  по своим находкам: 8, затем 12, затем 11 — все убиты.
- **Отчёт.** `README.md`: запуск, что сделано, что не сделано, допущения,
  таблица сравнения двух прогонов, итоги аудита.
- **Репозиторий опубликован.** https://github.com/Domex666/dz1, ветка `main`.

## В работе

Ничего. Работа завершена, проект сдан.

## Осталось за рамками сдачи

Сознательные нецели и незакрытые вопросы — не «забыли», а решили не делать:

- **Подтверждение допущений человеком:** границы длин (тег 32, тегов 10,
  `title` 200, `content` 10000) выбраны агентом. Зафиксированы тестами.
- **Категории тегов** (`работа`/`work` → `WORK`) — открытый вопрос из `SPEC.md`.
- **Пагинация, полнотекстовый поиск, аутентификация, мягкое удаление, OpenAPI** —
  нецели, список в `SPEC.md`.

## Активные инструменты

- Docker, образ `php:8.4-cli` (PHP 8.4.24) — способ вызвать `php` на машине,
  где он не установлен, замена `brew install php`. Частью проекта не является:
  ни `Dockerfile`, ни `docker-compose.yml` в репозитории нет, код о контейнере
  не знает. **В «Известные проблемы» не входит осознанно — это не проблема.**
- PHPUnit 11.5.56, `tools/phpunit.phar`.
- Скилл `laravel-arch` — источник архитектуры. Сам Laravel не используется.
- Composer не используется, зависимостей у проекта нет.

## Известные проблемы

- **Мутация «атомарная запись → прямая» не убита тестом.** Воспроизвести оборванную
  запись в тесте не получилось; под нагрузкой аудит наблюдал 7 рваных чтений из 400.
  Открыта третью сессию подряд.
- **`Content-Type` не проверяется юнит-тестами.** В CLI `header()` не работает,
  `headers_list()` пуст. Закрыто сверкой в `scripts/smoke.sh` — но смоук страхует
  только те сценарии, которые в нём есть, и набор тестов не подменяет.
- **Блокировка сериализует любую запись** — узкое место под нагрузкой.
  Сама по себе работает: аудит прогнал 4 процесса по 25 записей, потерь нет.
- **Ключ блокировки не зависит от пути хранилища:** два экземпляра с разными файлами
  будут сериализоваться на одном lock-файле. Избыточная блокировка, не некорректность.
  Починка потребовала бы протащить путь в сервис — то есть вернуть протечку слоя.
- **`HEAD`, `OPTIONS` и заголовок `Allow` при `405` не поддержаны** — осознанно,
  вне объёма.
- **Нормализация юникода (NFC/NFD) не выполняется:** `é` в двух формах даст два
  визуально одинаковых тега. Нужен `intl`, а зависимостей у проекта нет.
- **Пагинации нет**, список отдаётся целиком.
- **Наивный прогон сделан в той же сессии**, где уже обсуждалась спека.
  Изоляция слабее требуемой, отмечено в `naive/README.md`.

## Следующий шаг

Нет. Работа завершена, репозиторий опубликован, все пункты обязательной части закрыты.
Если проект будут продолжать — начинать с открытых вопросов в `SPEC.md`
и с единственной непокрытой мутации, описанной выше.
